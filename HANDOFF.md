# Продолжение работы в новой сессии

Файл для того, чтобы не пересказывать проект заново. Дайте новому агенту
короткий промпт (в конце), он прочитает остальное сам.

## Что это

RentHUB — P2P-аренда строительного инструмента. Пилот: **Кокшетау**, одна
категория, закрытые приглашения.

| | |
|---|---|
| Папка | `C:\Мои проекты\Startup orda\Никита\renthub` |
| Приложение | https://mandaloriancs.github.io/renthub/app/ |
| Лендинг | https://mandaloriancs.github.io/renthub/ |
| Репозиторий | https://github.com/MandalorianCs/renthub |
| Supabase | проект `owfsfwqwulpossjbnprp`, регион Frankfurt |

Стек: Expo (React Native + TypeScript, expo-router), Supabase (Postgres +
Auth + Storage). Лендинг — отдельный статический HTML.

## Что читать в первую очередь

1. `README.md` — архитектура, Trust Score, миграции, скрипты
2. `DESIGN.md` — правила оформления и чек-лист для нового экрана
3. `PITCH.md` — правки в питч-деку
4. `AGENTS.md` — Expo сильно изменился, документацию версии читать до кода

## Как здесь всё устроено

**Бизнес-правила живут в Postgres**, а не в React: позже подключится
Telegram-бот, и правило в компоненте он обойдёт. Переходы статусов — только
через RPC, суммы проставляет триггер из цены объявления, пороги в
`app_settings`.

**Схема разворачивается пушем в `main`** — подключена GitHub-интеграция
Supabase. Отсюда правило: **не менять схему через SQL Editor**, иначе
следующий деплой падает с ошибкой синхронизации.

**Перед каждым пушем прогонять стенд.** Он поднимает чистый Postgres в
Docker, накатывает миграции и проезжает четыре сценария с включённым RLS:

```
npm run test:db
```

**Сайт обновляется отдельно** — `npm run build:pages` пересобирает `docs/`,
откуда GitHub Pages раздаёт лендинг и приложение.

## Рабочие команды

```
npm run web             локальный сервер
npm run test:db         стенд миграций (нужен Docker)
npm run typecheck       tsc
npm run build:pages     пересобрать публикацию
npm run invite -- +7701... "Имя"    завести участника, выдать пароль
npm run moderator -- +7701...       право разбирать споры
npm run demo:fill                   демо-объявления из demo-photos/
npm run demo:clear                  убрать их
```

Последние четыре требуют секретного ключа переменной окружения:

```
$env:SUPABASE_SECRET_KEY="sb_secret_..."; npm run invite -- +7701... "Имя"
```

## Чего агент делать не должен

- **Не брать в руки `sb_secret_`** — он обходит все политики RLS. Команды с
  ним запускает человек, агент только даёт текст команды.
- **Не писать телефоны и ключи в репозиторий** — он публичный.
- **Не менять схему через SQL Editor** — только новой миграцией и пушем.
- **Не ставить UI-киты** (Tamagui, gluestack, Material): они принесут свой
  визуальный язык вместо бренда из деки. Из открытого кода берём поведение
  и ресурсы, не внешний вид.

## Особенности окружения

- Терминал часто открывается в папке выше — перед npm-командами `cd` в
  `renthub`.
- Docker Desktop падал из-за компонента Docker AI; он выключен в настройках,
  копия старых настроек — `settings-store.json.backup`.
- SMS-провайдер не подключён: для Казахстана Twilio требует $250/мес только
  за регистрацию имени отправителя. Вход по приглашениям,
  `EXPO_PUBLIC_AUTH_MODE=invite`. Вкладка «По SMS» готова и включится
  сменой переменной.

## Что осталось нерешённым

- **Интервью с владельцами** — ни одного, а на питче спросят
- **SMS-провайдер** — когда пилот перерастёт круг знакомых; подключается
  через Send SMS Hook к местному агрегатору, без привязки к Twilio
- **Демо-фото** — положить снимки инструмента в `demo-photos/` и запустить
  `npm run demo:fill`
- **Дека** — слайд «Бизнес-модель» противоречит продукту, текст замены
  готов в `PITCH.md`
- **Сумма при авторешении спора** — сейчас меньшее из ущерба и депозита
- **Минимум фото при публикации** — сейчас одно

## Промпт для новой сессии

> Проект RentHUB: `C:\Мои проекты\Startup orda\Никита\renthub`
>
> Прочитай `HANDOFF.md`, затем `README.md` и `DESIGN.md` — там всё
> состояние проекта, правила и открытые вопросы. Дальше продолжай работу.
>
> Терминал может открыться в папке выше — делай `cd` в `renthub` перед
> npm-командами. Секретный ключ Supabase в руки не бери: команды с ним
> запускаю я сам.
